BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Company] ADD [membership] VARCHAR(50) NOT NULL CONSTRAINT [Company_membership_df] DEFAULT 'TRIAL';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
