BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[Company] DROP CONSTRAINT [Company_gst_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
